{
  "name": "onlyrealty",
  "version": "1.0.0",
  "private": true,
  "description": "Static listing site for 17 Chelmsford Avenue — local preview helper",
  "scripts": {
    "preview": "serve -s . -l 3000",
    "preview:port": "serve -s . -l $PORT"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
